# Normalize to LF in the repo and on checkout (builds run in Linux containers).
* text=auto eol=lf

# Spool fixtures are byte-exact test inputs — never rewrite line endings.
src/test/resources/fixtures/** binary

*.jar binary
*.png binary
